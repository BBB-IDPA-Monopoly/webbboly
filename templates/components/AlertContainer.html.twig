{% if this.hasAlerts %}
    {% for type, messages in this.alerts %}
        {% for message in messages %}
            {{ component('Alert', {
                type: type,
                message: message,
                dismissible: this.dismissible,
            })}}
        {% endfor %}
    {% endfor %}
{% endif %}
