<div id="field{{ field.actionField.position }}" class="{{ this.classes }}">
    <div class="cell-content">
        <span>{{ field.actionField.name }}</span>

        {% block content %}{% endblock %}
    </div>

    {{ include('game/partials/_visitor.html.twig') }}
</div>
