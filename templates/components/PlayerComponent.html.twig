<div id="player{{ player.id }}" class="card {% if isMe %}bg-info{% endif %}">
    <div class="card-body">
        <div class="card-title">
            <h6>{{ player.nickname }}</h6>
        </div>
        <p>{{ player.money }}</p>
    </div>
</div>
