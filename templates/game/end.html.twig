{% extends 'game/layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Game over #{{ game.code }}</h1>
                <p>Winner: {{ winner.nickname }}</p>
                <p>Players:</p>
                <ul>
                    {% for player in players %}
                        <li>{{ player.nickname }}</li>
                    {% endfor %}
                </ul>
            </div>
            <a href="{{ path('app_index') }}" class="btn btn-primary">Zurück zur Startseite</a>
        </div>
    </div>
{% endblock %}

{% block title %}
    Game over #{{ game.code }}
{% endblock %}

