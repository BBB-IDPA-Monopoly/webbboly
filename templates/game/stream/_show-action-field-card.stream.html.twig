<turbo-stream action="update" target="modal">
    <template>
        {% include 'game/partials/_buy-action-field-modal.html.twig' %}

        <div
            {{ stimulus_controller('bootstrap-modal', {
                modalId: 'actionFieldCardModal',
                state: 'open',
            }) }}
        ></div>
    </template>
</turbo-stream>
