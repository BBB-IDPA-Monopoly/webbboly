<turbo-stream action="replace" target="player{{ player.id }}">
    <template>
        {{ component('PlayerComponent', {
            'player': player,
            'isMe': isMe,
            'isHost': player.isHost,
            'isMyTurn': isMyTurn,
        }) }}
    </template>
</turbo-stream>
