{% extends 'base.html.twig' %}

{% block title %}Spitzname{% endblock %}

{% block body %}
    <div class="p-5 text-center" style="height: 100vh;">
        <div class="h-50">
            <h1 class="fw-bold display-1">Webbboly</h1>
            {{ component('FlashContainerComponent') }}
        </div>
        <div class="h-50">
            <div class="h-50">
                {{ form_start(nicknameForm) }}
                <div class="col-lg-4 mx-auto">
                    <div class="input-group input-group-lg has-validation">
                        {{ form_row(nicknameForm.nickname) }}
                        <button class="btn btn-success" type="submit">Speichern</button>
                    </div>
                </div>
                {{ form_end(nicknameForm) }}
            </div>
        </div>
    </div>
{% endblock %}
