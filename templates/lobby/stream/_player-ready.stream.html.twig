<turbo-stream action="replace" target="player-{{ player.id }}">
    <template>
        {{ component('LobbyPlayer', {
            'player': player,
            'isMe': false,
            'isHost': player.isHost,
            'forHost': forHost,
        }) }}
    </template>
</turbo-stream>
